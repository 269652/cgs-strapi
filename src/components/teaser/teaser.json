{
  "collectionName": "teasers",
  "info": {
    "displayName": "Teaser",
    "icon": "star"
  },
  "attributes": {
    "variant": {
      "type": "enumeration",
      "enum": ["classic", "modern", "articleClassic", "articleModern", "text"],
      "default": "classic"
    },
    "title": {
      "type": "string",
      "required": true
    },
    "image": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": ["images"]
    },
    "copy": {
      "type": "text",
      "required": false
    },
    "ctaLink": {
      "type": "string"
    },
    "ctaLabel": {
      "type": "string"
    }
  }
}
